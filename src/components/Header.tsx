import React from "react";
import { SocialIcon } from "react-social-icons";

type Props = {};

const Header = (props: Props) => {
	return (
		<header>
			<div>{/*Social Icons*/}I am Header</div>
		</header>
	);
};

export default Header;
